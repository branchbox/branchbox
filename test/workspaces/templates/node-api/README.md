# Node API Sample

Minimal Node.js HTTP server for verifying BranchBox devcontainer behaviour.

## Suggested Checks

1. `branchbox init --stack nodejs`
2. `branchbox feature start smoke-run`
3. Reopen both worktrees in a container-capable editor.
4. Run `npm install` then `npm start` inside the feature container to verify runtime installs cleanly.

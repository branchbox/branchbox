# Rust CLI Sample

Minimal Rust application used to verify BranchBox devcontainer workflows.

## Suggested Checks

1. `branchbox init --stack rust`
2. `branchbox feature start smoke-run`
3. Open both worktrees in a devcontainer-capable editor.
4. Run `cargo run` in the feature worktree to ensure the container is healthy.
